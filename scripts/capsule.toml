# [rust]
# # path of rust contracts workspace directory,
# # a `Cargo.toml` file is expected under the directory.
# workspace_dir = "."

# capsule version
version = "0.10.5 04fd58c"

[[contracts]]
name = "ickb_logic"
template_type = "Rust"

[[contracts]]
name = "owned_owner"
template_type = "Rust"

[[contracts]]
name = "limit_order"
template_type = "Rust"
