# [rust]
# # path of rust contracts workspace directory,
# # a `Cargo.toml` file is expected under the directory.
# workspace_dir = "."

# capsule version
version = "0.10.4 eff02bf"

[[contracts]]
name = "ickb_logic"
template_type = "Rust"
